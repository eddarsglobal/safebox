# SafeBox v0.2.2B Name Fix R8

R8 is a formatting-only correction over R7.

- Product behavior unchanged from R7.
- Fixed the remaining `cargo fmt --check` deviation in `safebox-desktop/src-tauri/src/lib.rs`.
- R8 verifier: `verification/verify_v022b_name_fix_r8.sh`.
- Requires execution on the reference Mac for Rust/Tauri validation.
